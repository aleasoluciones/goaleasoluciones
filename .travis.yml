language: go
go:
 - 1.17.x

sudo: false

# safelist branch to build
branches:
 only:
  - master
  - 1.17

script:
 - make jenkins

notifications:
 email:
  - bifer@alea-soluciones.com

install:
 - sudo apt update
 - sudo apt install ca-certificates libgnutls30 -y
